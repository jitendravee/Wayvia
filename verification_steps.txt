VERIFICATION STEPS FOR THE FIX

1. **Selection Flow**:
   - User types "Ahmed" → suggestions appear (from countries.dev)
   - User selects "Ahmedabad" suggestion
   - handlePlaceSelect called:
     - setInputValue("Ahmedabad") → input shows "Ahmedabad"
     - setPlaceId("place_ahmedabad") → stored internally
     - setOpen(false), setHighlight(0)
     - onChange("Ahmedabad") ← CORRECT: sends name as query
   - Parent re-renders with value = "Ahmedabad"
   - inputValue useEffect: value === inputValue, so no change
   - resolved place lookup useEffect: runs because inputValue changed to "Ahmedabad"
     - Calls /api/places?q=Ahmedabad&limit=8
     - Gets result: { id: "place_ahmedabad", name: "Ahmedabad", state: "Gujarat", country: "India", ... }
     - Sets resolvedId = "place_ahmedabad", resolvedName = "Ahmedabad"
   - showCaption = true (shouldShowPlaceName && !open && !!resolvedName)
   - Input shows: "Ahmedabad"
   - Under input shows: "Ahmedabad, Gujarat, India"

2. **Typing Flow**:
   - User types "p" → inputValue = "p"
   - handleInputChange: setInputValue("p"), setPlaceId(null), onChange("p")
   - usePlaceSearch: query length < 2, so no suggestions
   - resolved place lookup: runs but inputValue.length < 2? No, it still runs but will get no results (or minimal)
   - User types "pu" → inputValue = "pu"
   - usePlaceSearch: now length >= 2, debounced search starts
   - After 300ms: calls /api/places?q=pu → gets suggestions
   - Suggestions appear in dropdown
   - No resolved name shown under input because suggestions are open (!open is false)

3. **Post-Selection Typing**:
   - After selecting "Ahmedabad", input shows "Ahmedabad", placeId = "place_ahmedabad"
   - User types "a" → inputValue becomes "Ahmedabada"
   - handleInputChange: setInputValue("Ahmedabada"), setPlaceId(null) ← clears internal ID
   - onChange("Ahmedabada") → parent gets the current query
   - usePlaceSearch: searches for "Ahmedabada" suggestions
   - resolved place lookup: runs with query "Ahmedabada" → may get different results or none
   - showCaption: false because suggestions are open (if any exist) OR if no suggestions but we still don't show resolved name because the condition is !open && !!resolvedName - if suggestions are closed but we have resolvedName from previous lookup, we might show it. But we cleared placeId, not resolved state. Actually, resolved state is still from previous lookup until the new lookup completes.
   - This is correct: while typing, we don't show the resolved place name under the input - we show suggestions or nothing.

4. **Architecture Separation**:
   - Place suggestions: /api/places?q=<inputValue> → countries.dev/cities?q=<inputValue> (only)
   - No calls to eRail, Ixigo, etc. during autocomplete
   - Internal place ID (placeId) is never sent via onChange or used in API calls
   - onChange always carries a place name/query string (for resolver)
   - Parent/resolver can use onChange value to call /api/places for resolved details
   - Journey search can use internal placeId (set during selection) to look up stations/bus/airport

5. **Edge Cases**:
   - Empty input: clears placeId, no suggestions, no resolved name
   - Invalid place name: /api/places returns empty results → resolvedId/name cleared
   - Debounce: 300ms on search via usePlaceSearch hook
   - Minimum query: 2 characters for suggestions
   - Stale request prevention: via TanStack Query keys
   - Caching: 5min stale time, 15min cache time

FILES MODIFIED:
- app/components/StationInput.tsx: Fixed selection bug, separated input value from internal ID, corrected resolved place lookup to use name not ID
- lib/hooks/usePlaceSearch.ts: Already correct (debounced, min length 2, caching)
- lib/query/places.ts: Legacy hook - unchanged but compatible (uses /api/places for name queries)
- app/api/places/route.ts: Already correct (only countries.dev, no fallbacks)

RESULT:
- User can type freely without auto-selection or incorrect population
- Selecting suggestion shows place name in input, sends name via onChange for resolver
- Internal place ID stored separately for journey search
- Zero transport provider calls during autocomplete
- Proper debouncing, caching, and error handling